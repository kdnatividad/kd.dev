const Blogs = () => {
  return (
    <section className="flex items-center justify-between gap-10">
      <div> {/* left - text */} </div>
      <div> {/* right - image */} </div>
    </section>
  );
};

export default Blogs;
